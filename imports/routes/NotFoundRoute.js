// @flow

import NotFound from '../components/NotFound'
import { type Route } from '../types'

/* eslint-disable global-require */
const route: Route = { component: NotFound }
/* eslint-enable global-require */

export default route
